import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'
import EpicFreeGames from './EpicFreeGames'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <EpicFreeGames />
  </React.StrictMode>,
)
